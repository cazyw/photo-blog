<nav class="navbar navbar-toggleable-md navbar-inverse fixed-top bg-inverse">
    <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
        aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

    <%= link_to 'Photo Blog', root_path, class: 'navbar-brand' %>
    

    <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
        <% if user_signed_in? %>
            <div class="navbar-nav">
                <%= link_to 'New Post', new_post_path, class: 'nav-item nav-link active' %>>
            </div>
            <form class="form-inline my-2 my-lg-0">
                <button class="btn btn-outline-warning my-2 my-sm-0" type="button"><%= link_to 'Logout', destroy_user_session_path, method: :delete %></button>
            </form>
        <% else %>
            <form class="form-inline my-2 my-lg-0">
                <button class="btn btn-outline-success my-2 my-sm-0" type="button"><%= link_to 'Login', new_user_session_path %></button>
                <button class="btn btn-outline-primary my-2 my-sm-0" type="button"><%= link_to 'Register', new_user_registration_path %></button>
            </form>
        <% end %>
    </div>
</nav>